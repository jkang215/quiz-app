const createUser = (req, res, next) => {
  // TODO: save created user as the value of the res.locals.user key
  // req.body will have the key displayName which should be saved to the user model
};

module.exports = { createUser };
